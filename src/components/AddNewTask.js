import React, { Component } from 'react';

export default class AddNewTask extends Component {
  // static propTypes = {}
  // static defaultProps = {}
  // state = {}

  render() {
    return (
      <div>
        <h1>
          AddNewTask
        </h1>
      </div>
    );
  }
}